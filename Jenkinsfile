pipeline{
  agent any
  checkout([$class: 'GitSCM', branches: [[name: '*/Vulkan']], extensions: [], userRemoteConfigs: [[credentialsId: '1', url: 'https://github.com/Krusto/FikoEngine']]])
  stages {
  }
}
